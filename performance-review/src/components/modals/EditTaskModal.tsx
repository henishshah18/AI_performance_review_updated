import React from "react"; import Modal from "../common/Modal"; interface EditTaskModalProps { isOpen: boolean; onClose: () => void; onSubmit: (data: any) => void; goals: any[]; task: any; } export default function EditTaskModal({ isOpen, onClose, onSubmit, goals, task }: EditTaskModalProps) { return <Modal isOpen={isOpen} onClose={onClose} title="Edit Task"><div>Edit Task Form</div></Modal>; }
