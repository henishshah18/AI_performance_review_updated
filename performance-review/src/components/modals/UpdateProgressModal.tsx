import React from "react"; import Modal from "../common/Modal"; interface UpdateProgressModalProps { isOpen: boolean; onClose: () => void; onSubmit: (data: any) => void; task: any; } export default function UpdateProgressModal({ isOpen, onClose, onSubmit, task }: UpdateProgressModalProps) { return <Modal isOpen={isOpen} onClose={onClose} title="Update Progress"><div>Update Progress Form</div></Modal>; }
