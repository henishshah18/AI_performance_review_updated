import React from "react"; import Modal from "../common/Modal"; interface CreateTaskModalProps { isOpen: boolean; onClose: () => void; onSubmit: (data: any) => void; goals: any[]; } export default function CreateTaskModal({ isOpen, onClose, onSubmit, goals }: CreateTaskModalProps) { return <Modal isOpen={isOpen} onClose={onClose} title="Create Task"><div>Create Task Form</div></Modal>; }
